apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fleet-commander.fullname" . }}-test-connection"
  labels:
    {{- include "fleet-commander.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fleet-commander.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
